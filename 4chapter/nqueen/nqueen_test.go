package nqueen

import (
	"fmt"
	"testing"
)

func TestQueue(t *testing.T) {
	// pos := 0
	// upperLim := 1<<uint32(8) - 1
	// pos = upperLim & (^(0 | 0 | 0))
	// mostRightOne := pos & (^pos + 1)

	a := num2(8)
	fmt.Println("------", a)
}
